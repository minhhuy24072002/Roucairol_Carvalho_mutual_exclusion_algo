public class RC_Protocol {
    public static void receiveRequest(NodeConfig node_config, Message message) {
        synchronized (node_config) {
            if (node_config.status == "IDLE") {

            }
        }
    }
}
